# texup

Local CLI tool that auto-remasters old game textures: scans a game folder,
classifies textures (diffuse / normal / UI / ...), upscales each with a
suitable super-resolution model, re-compresses to the original format and
writes results to an output folder. Optional apply/rollback with backups.

Status: early development.
